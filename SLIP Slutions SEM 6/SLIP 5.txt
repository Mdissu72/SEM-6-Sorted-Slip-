📚 Q.1 Advanced Java:

✅ A) JSP Program to Calculate Sum of First and Last Digit of a Given Number

Directory Structure
pgsql
Copy
Edit
/SumDigits
├── /WEB-INF
│   └── web.xml
└── index.jsp

index.jsp

<%@ page contentType="text/html; charset=UTF-8" language="java" %>
<html>
<head>
    <title>Sum of First and Last Digit</title>
</head>
<body>
    <h2>Enter a Number to Calculate Sum of First and Last Digit:</h2>
    <form action="index.jsp" method="post">
        <input type="text" name="number" required>
        <input type="submit" value="Calculate">
    </form>

    <%
        String numStr = request.getParameter("number");
        if (numStr != null && !numStr.isEmpty()) {
            int num = Integer.parseInt(numStr);
            int lastDigit = num % 10;
            int firstDigit = Integer.parseInt(Character.toString(numStr.charAt(0)));
            int sum = firstDigit + lastDigit;
    %>
        <h3 style="color: red; font-size: 18px;">
            Sum of First and Last Digit: <%= sum %>
        </h3>
    <% } %>
</body>
</html>


✅ Run Instructions:
Place files in webapps/SumDigits.

Run Tomcat and access:


http://localhost:8080/SumDigits/index.jsp

✅ B) Java Program in Multithreading Using Applet for Traffic Signal
TrafficSignalApplet.java

import java.applet.*;
import java.awt.*;

public class TrafficSignalApplet extends Applet implements Runnable {
    private String currentSignal = "RED";
    private Thread t;

    public void init() {
        t = new Thread(this);
        t.start();
    }

    public void paint(Graphics g) {
        g.setColor(Color.BLACK);
        g.drawRect(50, 50, 100, 300);
        g.drawRect(60, 60, 80, 80);
        g.drawRect(60, 150, 80, 80);
        g.drawRect(60, 240, 80, 80);

        if (currentSignal.equals("RED")) {
            g.setColor(Color.RED);
            g.fillOval(70, 70, 60, 60);
        } else {
            g.setColor(Color.BLACK);
            g.fillOval(70, 70, 60, 60);
        }

        if (currentSignal.equals("YELLOW")) {
            g.setColor(Color.YELLOW);
            g.fillOval(70, 160, 60, 60);
        } else {
            g.setColor(Color.BLACK);
            g.fillOval(70, 160, 60, 60);
        }

        if (currentSignal.equals("GREEN")) {
            g.setColor(Color.GREEN);
            g.fillOval(70, 250, 60, 60);
        } else {
            g.setColor(Color.BLACK);
            g.fillOval(70, 250, 60, 60);
        }
    }

    public void run() {
        while (true) {
            try {
                currentSignal = "RED";
                repaint();
                Thread.sleep(3000);

                currentSignal = "YELLOW";
                repaint();
                Thread.sleep(2000);

                currentSignal = "GREEN";
                repaint();
                Thread.sleep(3000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }
}


✅ HTML to Run Applet

<!DOCTYPE html>
<html>
<head>
    <title>Traffic Signal Applet</title>
</head>
<body>
    <h2>Traffic Signal Simulation</h2>
    <applet code="TrafficSignalApplet.class" width="200" height="400"></applet>
</body>
</html>


📱 Q.2 Android:

✅ A) Android Application to Change Color, Font Size, and Font Style on Button Click

MainActivity.java

package com.example.textstyleapp;

import android.graphics.Color;
import android.graphics.Typeface;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {
    TextView collegeName;
    Button changeStyleBtn;
    boolean isChanged = false;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        collegeName = findViewById(R.id.collegeName);
        changeStyleBtn = findViewById(R.id.changeStyleBtn);

        changeStyleBtn.setOnClickListener(v -> {
            if (!isChanged) {
                collegeName.setTextColor(Color.RED);
                collegeName.setTextSize(24);
                collegeName.setTypeface(Typeface.SERIF, Typeface.BOLD_ITALIC);
                isChanged = true;
            } else {
                collegeName.setTextColor(Color.BLUE);
                collegeName.setTextSize(18);
                collegeName.setTypeface(Typeface.SANS_SERIF, Typeface.NORMAL);
                isChanged = false;
            }
        });
    }
}

activity_main.xml

<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/collegeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ABC College"
        android:textSize="20sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"/>

    <Button
        android:id="@+id/changeStyleBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Change Style"
        android:layout_below="@id/collegeName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"/>
</RelativeLayout>


✅ B) Android Application to Find Factorial and Display Result in Alert Box

MainActivity.java

package com.example.factorialapp;

import android.app.AlertDialog;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {
    EditText numberInput;
    Button calculateBtn;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        numberInput = findViewById(R.id.numberInput);
        calculateBtn = findViewById(R.id.calculateBtn);

        calculateBtn.setOnClickListener(v -> {
            String numStr = numberInput.getText().toString();
            if (!numStr.isEmpty()) {
                int num = Integer.parseInt(numStr);
                long factorial = calculateFactorial(num);
                showResult(num, factorial);
            }
        });
    }

    private long calculateFactorial(int n) {
        long fact = 1;
        for (int i = 1; i <= n; i++) {
            fact *= i;
        }
        return fact;
    }

    private void showResult(int number, long result) {
        AlertDialog.Builder builder = new AlertDialog.Builder(this);
        builder.setTitle("Factorial Result");
        builder.setMessage("Factorial of " + number + " is: " + result);
        builder.setPositiveButton("OK", (dialog, which) -> dialog.dismiss());
        builder.show();
    }
}

activity_main.xml

<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/numberInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Enter Number"
        android:inputType="number"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"/>

    <Button
        android:id="@+id/calculateBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Calculate Factorial"
        android:layout_below="@id/numberInput"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"/>
</RelativeLayout>
