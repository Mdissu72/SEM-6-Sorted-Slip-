📚 Q.1 Advanced Java:

✅ A) Java Program to Display Employee Names Starting with 'A'
EmployeeFilter.java

import java.util.ArrayList;
import java.util.List;

public class EmployeeFilter {
    public static void main(String[] args) {
        // List of employee names
        List<String> employees = new ArrayList<>();
        employees.add("Amit");
        employees.add("Rahul");
        employees.add("Anjali");
        employees.add("Pooja");
        employees.add("Arjun");
        employees.add("Suresh");

        // Display employees whose names start with 'A'
        System.out.println("Employees whose names start with 'A':");
        for (String name : employees) {
            if (name.startsWith("A")) {
                System.out.println(name);
            }
        }
    }
}

✅ Run Instructions:
Save as EmployeeFilter.java.

Compile and run:

javac EmployeeFilter.java
java EmployeeFilter

✅ B) Java Program in Multithreading Using Applet for Digital Watch

DigitalWatch.java

import java.applet.Applet;
import java.awt.*;
import java.text.SimpleDateFormat;
import java.util.Date;

public class DigitalWatch extends Applet implements Runnable {
    private Thread t;
    private String time;

    public void init() {
        setSize(300, 150);
        t = new Thread(this);
        t.start();
    }

    public void paint(Graphics g) {
        g.setFont(new Font("Arial", Font.BOLD, 30));
        g.setColor(Color.BLUE);
        g.drawString(time, 50, 80);
    }

    public void run() {
        try {
            while (true) {
                Date now = new Date();
                SimpleDateFormat formatter = new SimpleDateFormat("hh:mm:ss a");
                time = formatter.format(now);
                repaint();
                Thread.sleep(1000);
            }
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }
}

✅ HTML to Run Applet

<!DOCTYPE html>
<html>
<head>
    <title>Digital Watch Applet</title>
</head>
<body>
    <h2>Digital Watch</h2>
    <applet code="DigitalWatch.class" width="300" height="150"></applet>
</body>
</html>


✅ Compile and Run:
Save DigitalWatch.java and compile:


javac DigitalWatch.java
Open the HTML file in your browser.


📱 Q.2 Android:

✅ A) Android Application to Demonstrate Phone Call Using Implicit Intent

MainActivity.java

package com.example.phonecallapp;

import android.content.Intent;
import android.net.Uri;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {
    Button callButton;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        callButton = findViewById(R.id.callButton);

        callButton.setOnClickListener(v -> {
            Intent intent = new Intent(Intent.ACTION_DIAL);
            intent.setData(Uri.parse("tel:+919876543210"));
            startActivity(intent);
        });
    }
}

activity_main.xml

<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/callButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Make a Call"
        android:layout_centerInParent="true"/>
</RelativeLayout>


✅ B) Android Application to Create Custom Alert Dialog with Friend Names and Greet Accordingly

MainActivity.java

package com.example.frienddialogapp;

import android.app.AlertDialog;
import android.content.DialogInterface;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {
    Button showDialogButton;
    String[] friends = {"Amit", "Rahul", "Neha", "Pooja"};

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        showDialogButton = findViewById(R.id.showDialogButton);

        showDialogButton.setOnClickListener(v -> showFriendDialog());
    }

    private void showFriendDialog() {
        AlertDialog.Builder builder = new AlertDialog.Builder(this);
        builder.setTitle("Choose a Friend");

        builder.setItems(friends, (dialog, which) -> {
            String selectedFriend = friends[which];
            greetFriend(selectedFriend);
        });

        builder.setNegativeButton("Cancel", (dialog, which) -> dialog.dismiss());
        builder.show();
    }

    private void greetFriend(String name) {
        AlertDialog.Builder builder = new AlertDialog.Builder(this);
        builder.setTitle("Greeting");
        builder.setMessage("Hello, " + name + "! How are you?");
        builder.setPositiveButton("OK", (dialog, which) -> dialog.dismiss());
        builder.show();
    }
}

activity_main.xml

<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/showDialogButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Show Friends List"
        android:layout_centerInParent="true"/>
</RelativeLayout>